{
  "name": "big",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev",
    "build": "cd server && npm install && cd ../client && npm install && rm -rf dist && cd ../client && npm run build && cp -r dist ../server && cd ../server && npm run build",
    "deploy": "npm run build && git add . && git commit -m finishedBuild && git push",
    "start": "cd server && npm run prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
